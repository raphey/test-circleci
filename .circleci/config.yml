version: 2.0

jobs:
  build:
    docker:
      - image: circleci/node:8.9.4
    steps:
      - setup_remote_docker
      - run: echo 'Hello World!'; echo 'This is the delivery pipeline'
      - run:
          name: Step to print the node version
          command: |
            node -v
            echo 'yay, printed node version'
