version: 2.0

jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      -checkout
      -run:
          name: The First step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
